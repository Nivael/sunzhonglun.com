# PRD — sunzhonglun.com 个人网站

> 本文档是本项目的唯一需求源。不在 PRD 里的功能，先改 PRD 再动手。
> 每个版本完成后，更新对应章节的状态标记，并在 CLAUDE.md「迭代记录」追加一条。

- **状态标记**：⬜ 未开始 · 🔨 进行中 · ✅ 已完成
- **最后更新**：2026-07-24

---

## 1. 产品定位

Zhonglun 的个人 portfolio 网站 + 写作存档：

- **是什么**：收录 92 篇过往微信公众号文章、之后的新写作、视频作品的个人站，同时作为对外展示自己的 portfolio 门面。
- **给谁看**：对我感兴趣的读者、潜在合作者、朋友。
- **核心体验**：极简高级的阅读体验（现有设计基因），加上足以承担 portfolio 职能的信息架构（导航、侧栏、自我介绍）。
- **长期原则**：内容为主，工程轻量，不引入不必要的依赖；所有迭代落文档。

## 2. 现状（2026-07-13）

- Astro 5 静态站已就绪（v0 基础站，2026-07-12 从 Python 生成器迁移完成）
- 内容：2 篇示例占位文章 + 关于页
- 设计：Medium 式极简，单栏 44rem，无侧栏，顶栏只有 文章/关于/主题切换
- ~~未部署，无远程仓库（纯本地 git），域名未购买~~ → 2026-07-13：GitHub 公开仓库
  [Nivael/sunzhonglun.com](https://github.com/Nivael/sunzhonglun.com) 已建并推送；Vercel 已部署；
  域名 sunzhonglun.blog 已购，DNS 记录待在 Cloudflare 添加（见 §4.3）

---

## 3. 版本路线图

版本号规则见 §9。按依赖关系排序，**先上线、再填内容、再升级设计**——尽早部署可以让每一步的效果都在真实环境验证。

| 版本 | 内容 | 状态 |
|------|------|------|
| v0 | Astro 迁移（基础站） | ✅ 2026-07-12 |
| v1.0 | 上线：GitHub 仓库 + Vercel 部署 + 域名 + 基础 SEO | ✅ 2026-07-13 |
| v1.1 | 公众号文章迁移（实际 76 篇，见 §5） | ✅ 2026-07-13 |
| v1.2 | 设计升级：portfolio 化（侧栏 + 四分类 + 新首页） | ✅ 2026-07-13 |
| v1.3 | 视频页 | ⬜ |
| v1.4 | 访问统计（侧栏 UV/PV，自建，见 §7.5；2026-07-19 提出，提前于 v1.3 实施） | ✅ 2026-07-21（PR #5 合并，线上数字待观察） |
| v1.5 | 设计升级二期：杂志化（首页/分类页网格改版 + 题图 + 滚动特效，见 §7.6） | ✅ 2026-07-23 |
| v1.6 | 英文版：LLM 逐篇文学翻译 + 双语路由（见 §7.7） | ✅ 2026-07-23 |
| v1.7 | 入口封面：《去创造》线条海 canvas 动效（见 §7.8） | 🔨 2026-07-24 |
| v2.x | 之后再规划（见 §10 非目标里的候选） | — |

---

## 4. v1.0 上线（域名 + 部署）

### 4.1 GitHub 仓库

- **公开**仓库（用户已确认，GitHub 账号 Nivael），推送现有历史。
- 推送时把默认分支从 `master` 更名为 `main`（一次性，与协作规则 §8 对齐）。

### 4.2 域名购买

- **正式域名：`sunzhonglun.blog`**（2026-07-13 已在 Cloudflare Registrar 购入）。
- 原属意的 `sunzhonglun.com`（用户早年注册后过期释放）已于 2025-07-01 被抢注
  （注册商 Cloud Yuqu LLC，NS 停放在 domain-expired 页，2027-07 到期）——
  属于域名商人待售状态，将来可在聚名/西部数码等交易平台尝试回购，回购成功后再做 301 迁移，
  站点架构上只需改 site.json 与 astro.config 各一行。
- 站点部署在海外（Vercel），**不需要 ICP 备案**。

### 4.3 Vercel 部署

- 免费版足够：静态站、全球 CDN、git push 自动部署。
- 配置：Framework = Astro（自动识别），构建命令 `npm run build`，输出 `dist/`。（2026-07-13 已部署）
- 域名绑定：`sunzhonglun.blog` 与 `www.sunzhonglun.blog`，在 Cloudflare DNS 各加一条 CNAME
  指向 Vercel 提供的目标值，**代理关闭（灰云 DNS only）**。主域名与 www 的重定向方向在 Vercel
  Domains 里配置，取裸域为主。
- 预览环境：每个 PR 自动生成 preview URL，作为设计/内容改动的验收入口。

### 4.4 基础 SEO 与站点设施

- `@astrojs/sitemap` 生成 sitemap.xml（这是「必要依赖」，允许引入）
- 每页 Open Graph / Twitter Card meta（标题、描述、类型）
- favicon（简单文字或首字母即可，不必等设计升级）
- 404 页面（复用 Base 布局）
- `robots.txt`
- 访问统计：**暂缓**，列入 §10 候选（保持零脚本负担；上了 Vercel 可先用其自带 Analytics 免费档）

### 4.5 验收标准

- [x] https://sunzhonglun.blog 可访问，HTTPS 正常（2026-07-13 验证 200）
- [x] push 到 main 后自动部署成功
- [x] feed.xml、sitemap.xml、robots.txt、favicon、404、OG meta 就绪
- [ ] 跳转方向翻转：目前是裸域 308 → www，需在 Vercel Domains 里改为 www → 裸域
  （canonical/sitemap/OG 均按裸域生成，保持一致）——**用户在 Vercel 点一下 Edit 即可**
- [ ] Google Search Console 提交收录（用户操作，可选、不阻塞）
- [ ] Lighthouse 移动端 Performance ≥ 95（待测，不阻塞）

---

## 5. v1.1 公众号文章迁移（92 篇）

### 5.1 方案

采用 **computer use（Claude in Chrome）爬取**为主的混合方案：

1. **首选直接抓取**：公众号文章的永久链接（`mp.weixin.qq.com/s/...`）通常无需登录即可访问，先用脚本直接 fetch 正文 HTML——快、可批量、可重跑。
2. **computer use 兜底**：遇到验证页、需要登录、或滚动加载图片的文章，用 Claude in Chrome 打开真实浏览器读取渲染后的页面。
3. 两条路径产出统一的中间格式（正文 HTML + 元数据），走同一套 HTML → markdown 转换。

### 5.2 流水线

```
URL 清单 (92 条)
  → 抓取正文 HTML + 标题 + 原发布日期
  → 清洗公众号私有标记（多层 section 嵌套、内联样式、data-* 属性）
  → 转 markdown（保留加粗/引用/列表/分隔线/代码）
  → 下载图片到 public/assets/images/<slug>/（请求头带 Referer: https://mp.weixin.qq.com/，绕防盗链）
  → 图片引用改写为 /assets/images/<slug>/xx.jpg
  → 写入 content/posts/YYYY-MM-DD-<slug>.md
  → 本地渲染人工校对
```

### 5.3 执行节奏

| 阶段 | 内容 | 验收 |
|------|------|------|
| 阶段 0 | 导出 92 篇清单，**每篇含：URL + 标题 + 原发表时间**，存 `docs/migration/articles.json`。方式：用户在后台「发表记录」页控制台运行 `docs/migration/fetch-articles.js`（Claude 扩展安全策略不允许访问 mp.weixin.qq.com，已验证，浏览器自动化路线不可行） | 清单齐、无重复、每篇都有发表时间 |
| 阶段 1 | 试点 3 篇（挑图多的、带引用的、纯文字的各一篇），人工逐篇校对 | 排版无破损，转换规则定稿 |
| 阶段 2 | 批量迁移，每批约 10 篇，进度记录在 `docs/migration/checklist.md`（每篇：URL / slug / 状态 / 备注） | 每批构建通过 + 抽查 |
| 阶段 3 | 全量校对收尾：日期、excerpt、slug 统一检查；删除 2 篇示例文章 | 92 篇全绿 |

### 5.4 元数据规则

- `date`：用公众号**原发布日期**，保持写作时间线真实
- `slug`：**英文语义短语**（用户已确认），小写连字符（如 `reading-notes`），迁移时逐篇定
- `quote`：**原文中最美的一句**，由 Claude 通读全文挑选，原样引用不改写——
  首页列表的预览位展示（衬线体 + 「」引号）
- `excerpt`：**一句话 trailer**，像预告片一样告诉读者这是一个关于什么的故事，
  由 Claude 撰写，不剧透——首页列表次行展示，并用于 og:description 与 RSS 摘要
- 文首不加"最初发布于公众号"注（试点阶段决定：保持正文干净）

### 5.5 验收标准

（总数修正：实际未删除文章 **76 篇**——后台"93"为发表记录数，含已删文章、无文章的群发，
以及 1 条重复记录：idx 31 与 idx 30 为同一篇《银河之一日》的两条发表记录）

- [x] 76 篇全部在新站正常渲染，图片全部本地化，无任何 `mp.weixin.qq.com` 资源依赖
- [x] 校对：用户已在 PR #2 审阅并合并
- [x] RSS 与首页列表按日期正确排序，构建时间可接受
- [x] 追加：每篇配 quote（原文最美一句，逐字校验）与 excerpt（一句话 trailer），见 §5.4

---

## 6. v1.2 设计升级（portfolio 化）

### 6.1 原则

**升级不推翻。** 现有设计基因（衬线正文、44rem 阅读版心、配色变量、全部微交互，见 CLAUDE.md「设计规范」）原样保留；升级的是**信息架构**，不是阅读体验。

### 6.2 参考站点（审美基准）

| 站点 | 借鉴点 |
|------|--------|
| brianlovin.com | 经典左侧 sidebar 双栏布局，导航/社交/分组清晰 |
| paco.me | 极简克制，留白与字体层级 |
| rauno.me | 微交互质感（craft 页尤其） |
| antfu.me | 开发者 portfolio 首页的自我介绍 + 内容混排 |
| leerob.com | 顶栏 + 内容型个人站的信息组织 |

落地前先出一版设计说明（布局草图 + 各断点行为），确认后再写码。

### 6.2.5 内容分类（2026-07-13 用户确认）

文章分四类：**Jazz / Portrait / Sketch / 文**。前三类由标题自动识别（标题含
"Jazz"、"Portrait"、"Sketch"，含 June Portrait 等变体），其余归入"文"。
分类由构建时从标题推导（`src/lib/posts.ts`），**不加 frontmatter 字段**——
数据结构上让错误不可能发生：标题即分类，无需手工维护第二份真相。
展示：侧栏分类导航 + 分类归档页（`/category/<slug>/`），首页列表在 meta 行显示分类。
侧栏分类区走大间距留白（条目间距约 1.15rem、字距微调），整体气质偏森系日系的安静疏朗。
设计草图（用户已过一轮意见）：https://claude.ai/code/artifact/5c9ea2da-0a4b-493b-a7e1-fa81b372829d

### 6.3 范围

- **顶栏**：Logo + 导航（文章 / 视频 / 关于）+ 主题切换；滚动时 sticky（是否毛玻璃背景，设计稿阶段定）
- **侧栏**（桌面端 ≥ 1080px 左侧固定）：头像、一句话简介 **"I write."**、导航、联系方式仅邮箱
  **zhonglunsun@gmail.com**（不放社交链接，用户已确认）、按年份的文章归档入口；窄屏折叠回顶栏
- **新首页**：hero 仅一句 **"Hi there, I'm Zhonglun, and I write."**（无副标题、无统计数字——内容持续更新，不做静态描述）+ 最新文章列表。
  **精选区暂缓**（2026-07-13 用户决定，未来要做时再回到本节）
- **文章页**：保持 44rem 阅读版心不变，侧栏在文章页收敛（或仅留窄导航），确保阅读零干扰
- **全站检查**：light/dark 两套、移动端、`prefers-reduced-motion`

### 6.4 验收标准

- [ ] 桌面/移动端截图走查（真实浏览器），两种主题各一遍
- [ ] 现有微交互全部仍在（进度条、渐现、hover 下划线、主题切换旋转）
- [ ] Lighthouse 不低于升级前

---

## 7. v1.3 视频页

- 新增 `videos` content collection：frontmatter 含 `title` / `date` / `platform`（bilibili | youtube）/ `id`（BV 号或 YouTube ID）/ 可选 `description`
- 视频页为卡片列表，点击展开内嵌播放器（B 站 iframe / YouTube embed），**懒加载**——默认只渲染封面图，点击才加载 iframe，保住加载性能
- 顶栏「视频」导航入口在 v1.2 已预留
- 具体交互到时候再细化，本节先占位

---

## 7.5 v1.4 访问统计（2026-07-19 从 §10 非目标移出）

### 需求

侧栏展示四个自动更新的数字：**总来访 / 今日来访**（UV，人）与 **总浏览 / 今日浏览**（PV，次）。
「今日」按东八区自然日，次日自动归零。

### 方案

- **存储**：Upstash Redis（Vercel Marketplace 免费档，用户在控制台开通并关联项目）。
  键：`uv:total`、`uv:day:<YYYY-MM-DD>`、`pv:total`、`pv:day:<YYYY-MM-DD>`；日键 TTL 3 天。
  日期口径东八区（无夏令时，UTC+8 直接偏移，见 `src/lib/visits.ts`）。
- **接口**：`api/visit.ts`——Vercel 根 `api/` 目录的 serverless 函数（不经 Astro 构建）。
  **零 npm 依赖**，直接以 fetch 调 Upstash REST pipeline。POST 计数并返回四个数字。
- **防污染**：仅 `VERCEL_ENV=production` 计数，preview/dev 只读；UA 含 bot/spider/crawl 等不计数。
- **UV 去重**：浏览器 localStorage 记当天标记（上报成功后才写入），同日重复访问只报 PV。
- **前端**：`src/scripts/main.js` 每页加载上报一次；侧栏存在占位元素时填充显示
  （即数字仅在首页/分类页的桌面端侧栏可见，但**所有页面的访问都计数**）。
- **降级**：无 Redis 环境变量或请求失败时接口返回 `ok:false`，前端保持统计区隐藏，页面无报错。

### 验收标准

- [ ] 生产环境四个数字随访问增长；同一浏览器当日重复访问 UV 不增、PV 增
- [ ] 「今日」数字在东八区次日归零
- [ ] Vercel preview 访问不改变计数
- [ ] 未开通 Redis 时站点表现与之前完全一致（统计区隐藏）
- [ ] `npm test` 覆盖日期口径边界（23:59/00:00 东八区）

### 用户操作

Vercel 控制台 → Storage（或 Marketplace）→ Upstash → 创建 Redis（免费档，区域选新加坡或美西均可）
→ Connect 到 sunzhonglun.com 项目（环境变量自动注入）→ Redeploy。

---

## 7.6 v1.5 设计升级二期（杂志化）

> 2026-07-21 立项。用户从四个候选参考中选定方向：**Gritty Culture Blog（Figma 官方 Sites 模板）
> 的杂志网格为主 + Miranda（niccolomiranda.com）的报刊编辑排版细节为辅**。
> 落选参考的处置：Stripe Press 的 3D 书架仅留作未来「精选区」（§6.3 暂缓项）的轻量化灵感；
> Nature Inspired Visual Essay 留作未来图片特稿的单页模板灵感。均不在本版本范围内。
> （注：立项时本地基线尚未包含 §7.5 访问统计，曾暂占 v1.4 编号，合流时更正为 v1.5。）

### 7.6.1 原则

沿用 §6.1「升级不推翻」：**文章阅读页版心零改动**；配色变量、衬线正文、深浅两套主题、
全部既有微交互（进度条、渐现、hover 下划线、主题切换旋转）与 §7.5 访问统计原样保留；
不引入任何新依赖，纯 CSS/原生 JS 实现。

### 7.6.2 范围

- **首页 masthead（Miranda）**：报刊式刊头——细线夹注的元信息行（写作存档年份 + 总篇数）
  + 放大的 hero 大字标题（文案不变）+ 粗细双线收尾
- **首页列表 → 杂志网格（Gritty）**：最新一篇为通栏 feature（大图 + 大标题）；
  其余文章两栏网格（≥1360px 三栏，窄屏单栏）。封面优先取可选 `cover`（用于恢复未进入正文的
  公众号题图），否则在构建时推导正文首图；两者都没有时以 quote 排版块代位。
- **分类页**：同款 masthead（分类名大字 + 篇数）+ 同款网格（无 feature）
- **不动**：文章页、关于页、分类总览页、RSS、侧栏结构

一轮截图验收通过后，二轮追加（2026-07-22 用户 Miranda 向反馈）：

- **通栏题图**：feature 升级为 masthead 下的通栏大图（21/9，≤820px 改 4/3），
  取最新一篇首图自动轮换；标题区改 7/5 双栏编辑排版（大标题 | 引文 + trailer）
- **滚动特效**：题图视差（rAF，位移 = 图高 12%，图预放大 1.12 留余量）；
  封面图与字卡进入视口时 clip-path 显影（复用 .reveal 的 IntersectionObserver）。
  均尊重 prefers-reduced-motion；惯性平滑滚动（Lenis 式）评估后落选——改动手感、收益有限
- **来访量手机端露出**：§7.5 的统计只展示在侧栏，而侧栏 <1080px 隐藏——页脚新增
  `.foot-stats`（来访/浏览总数，默认隐藏拿到数据后显示），与侧栏同源同请求，
  手机与桌面两端均可见。不引入第三方计数（曾评估 Vercount，因 §7.5 自建方案已上线而弃）

### 7.6.3 验收标准

- [x] 桌面/移动端截图走查，深浅两主题各一遍
- [x] 现有微交互与访问统计全部仍在；`prefers-reduced-motion` 正常
- [x] 网格首图全部懒加载（题图除外），`aspect-ratio` 占位无布局抖动
- [x] 77 篇文章均有列表封面；24 篇无正文插图的旧稿使用已本地化的公众号题图
- [x] 手机端页脚能看到来访/浏览数字（生产环境）
- [x] npm test 通过（含首图推导测试）；构建通过
- [x] 用户在 Vercel preview 验收后合并

---

## 7.7 v1.6 英文版（LLM 逐篇文学翻译）

> 2026-07-23 立项。英文版不是浏览器按钮触发的即时机翻，也不接第三方翻译 API。
> 每篇译文由了解本站完整语料与作者经历的 LLM 通读原文后单独翻译、校订，作为独立 Markdown
> 进入 git；英文站只展示已完成的译文。目标是 **faithful, yet unmistakably in Zhonglun's voice**。

### 7.7.1 翻译原则

- **忠实优先，但不逐字僵译**：事实、人物关系、意象、叙述视角与情绪落点不得改写；
  句法可为自然英文重组，不能为“流畅”擅自解释、删减或增添意义。
- **保留作者声音**：保住长短句交替、段落呼吸、克制的自我暴露、冷面自嘲，以及口语突然进入
  抒情或思辨的转折；不把文字磨平成中性的英语散文，也不美式化中国经验。
- **文化语境少解释**：人名、地名、书名与中国互联网语汇采用读者可辨认的译法；只有理解情节
  必需时才在句内轻量补足，不堆脚注。
- **元数据同样逐篇写**：英文 `title` 是文学标题，`quote` 必须逐字出现在译文正文，
  `excerpt` 是不剧透的英文 trailer；不能把中文元数据直接交给批量翻译器。
- **逐篇验收**：每篇依次做“事实对照 → 声音与节奏 → 专名与引文 → 英文润色”四遍校订；
  一篇一个可审阅单元。先以《去创造》定调，用户确认声音后再分批推进存量文章。

### 7.7.2 内容与路由

- 中文原文继续放在 `content/posts/`，是唯一源稿；译文放在 `content/posts-en/`，
  文件名必须与中文源稿完全一致，由同一 id 配对，不另维护易漂移的 slug/date。
- 英文首页 `/en/` 只列出已经翻译完成的文章；英文文章为 `/en/posts/<slug>/`，
  中文原 URL 保持不变。英文关于页为 `/en/about/`。
- 有对应译文时，中英文页面右上角显示语言入口。PC 端使用 `中文 / EN` 双选项 tab 并标明
  当前语言；移动端改为导航内的单一目标语言文字入口（中文页显示 `EN`，英文页显示 `中`），
  不另占一行、不使用胶囊底色。移动端站名已链接文章首页，因此隐藏重复的 `文章 / Writing`
  导航项，为中英文顶栏保留一致呼吸空间。没有译文的中文文章不显示虚假的语言入口。页面
  输出 `lang`、canonical 与双向 `hreflang`。
- 本站采用轻量 i18n 架构：语言由路由前缀、内容集合配对和 `Base.astro` 的 `lang` /
  `alternatePath` 共同确定，不引入运行时翻译字典或第三方 i18n 依赖。当前只有中英两种语言，
  且主要内容是独立 Markdown 译稿，这比完整 i18n 框架更直接；若未来增加第三种语言或大量
  共用 UI 文案，再将路由生成、文案字典与 fallback 抽成统一 i18n 模块。
- 图片继续复用 `/assets/images/`；无运行时 AI、无新增 npm 依赖，仍是 Astro 全静态生成。
- 新中文文章进入仓库时，协作流程默认检查并创建同名英文译稿；当前历史欠稿由
  `tests/translation-backlog.json` 明确登记，未登记的新中文稿若缺少译稿会令测试失败。
  若作者明确要求中文先发，需在同一变更中把该稿登记进 backlog；英文首页不会出现半成品。

### 7.7.3 验收标准

- [x] 《去创造》英文样稿完成四遍校订
- [x] 用户确认《去创造》译文声音，作为其余文章的定调样稿（2026-07-23）
- [x] `/en/`、`/en/about/`、`/en/posts/go-create/` 正常静态生成，语言切换双向可达
- [x] `html lang`、canonical、`hreflang="zh-CN"` / `hreflang="en"` 正确
- [x] 未翻译中文文章不出现 EN 切换，不会生成空英文页
- [x] 深浅主题、移动端、阅读进度与 reduced-motion 沿用现有体验
- [x] 测试覆盖中英文章配对与英文阅读时长；`npm test`、`npm run build` 通过
- [x] 测试以显式 backlog 区分历史欠稿与新稿；未来新增中文稿默认必须同轮提供英文稿
- [x] 77 篇存量文章全部完成四遍校订，翻译 backlog 清零
- [x] PC 双选项 tab 与移动端单一语言入口均无溢出、遮挡或导航挤压，键盘与读屏可辨认目标语言

进度：**77 / 77 篇**（2026-07-23），存量翻译完成，backlog 为空。全部文章按 §7.7.1
逐篇完成四遍校订，没有以批量机翻补齐数量。《Old Angel》中文源稿的正文原本即由作者
以英文写成，英文版保留原文，不以“润色”为名改写，作为作者原生英文声音的参照。

---

## 7.8 v1.7 入口封面（《去创造》线条海）

> 2026-07-24 用户提出：以《去创造》题图作全站封面——先见图，底部按钮进入，小方块在线条海上漂。
> 原图未在本地落盘（生成它的会话丢失），采用**程序化重绘**：canvas 按原风格实时绘制
> （种子 2006 可复现），不存静态大图，任意分辨率清晰，动效即绘制本身。

### 方案

- **位置**：中英两个首页顶部 100svh 通栏（`Cover.astro`，打破内容列横跨全视口）；
  分类页、文章页不加。masthead 与杂志网格紧随其下，滚动即达——封面不拦内容，SEO 无损
- **画面**：上疏下密的虚线渐变为波浪线（复刻题图），绿色小方块（--accent）贴浪漂浮、
  缓慢摇摆；深浅主题各自配色，主题切换即时重绘
- **文案**：底部说明行 EST. 2006 · WWW ｜ 去创造（EN：GO CREATE）｜ 000000 → 总篇数
  （计数动画）；「进 入」（EN：ENTER）按钮平滑滚动至刊头
- **性能与无障碍**：滚出视口自动暂停 rAF；prefers-reduced-motion 静止一帧、按钮不浮动；
  canvas 对读屏隐藏，aria-label 描述封面

### 验收标准

- [ ] 中英首页、深浅主题、桌面/移动截图走查；动效流畅无卡顿
- [ ] reduced-motion 下静止且可正常进入
- [ ] 构建与 npm test 通过；Lighthouse 不显著回退
- [ ] 用户在 Vercel preview 验收后合并

---

## 8. 协作规则

- **PRD 驱动**：功能不在 PRD 里就先改 PRD；产品决策变化（如砍掉某功能）也要回写 PRD，避免"undocumented 决策被推翻"。
- **分支与合并**：
  - 工程改动（代码、样式、结构）：`feature/*` 分支 → PR（GitHub 上线后走真 PR + Vercel preview 验收）→ merge 到 `main`
  - 纯内容更新（新文章、改错字）：可直接提交 `main`，不必开分支
- **提交信息**：中文，说清做了什么和为什么
- **每次迭代收尾三件事**：① CLAUDE.md「迭代记录」追加一条；② PRD 对应版本状态更新；③ 受影响的文档章节同步（结构 / 命令 / 待办）
- **验收方式**：用户在 dev server 或 Vercel preview 上过目后合并；纯文档改动可直接合并

## 9. 版本规则

- 版本号 `vX.Y`：**Y** = 路线图里的一次功能迭代（如 v1.1 内容迁移），**X** = 大改版（信息架构或技术栈级别的变化）
- 每个版本合并后打 **git tag**（如 `v1.0`），tag message 一句话概述
- CLAUDE.md「迭代记录」即 changelog，不另设 CHANGELOG 文件
- `dist/` 永不入库，构建交给 Vercel

## 10. 非目标（明确暂不做）

以下有意不做，避免范围膨胀；未来若要做，先回到本节把它移出去：

- 评论系统（可考虑的最轻方案是 giscus，v2 再议）
- 后台 CMS / 在线编辑（markdown + git 就是编辑器）
- Newsletter / 邮件订阅（RSS 已覆盖订阅需求）
- 全文搜索（92 篇量级下按年归档 + 浏览器查找够用，v2 再议）
- ~~访问统计~~ → 2026-07-19 移出非目标，见 §7.5（v1.4）

## 11. 开放问题

已答复（2026-07-13）：

| # | 问题 | 答复 |
|---|------|------|
| 1 | 仓库公开还是私有 | **公开**（GitHub 账号 Nivael） |
| 2 | 域名注册商 | **Cloudflare**，购买由用户操作 |
| 3 | 92 篇 URL 清单来源 | 公众号后台发表记录页，用户保持登录态，Claude 浏览器扩展爬取 |
| 4 | slug 风格 | **英文语义** |
| 5 | 侧栏信息 | 无社交链接；邮箱 zhonglunsun@gmail.com；简介 "I write." |

仍待定：

| # | 问题 | 阻塞哪个版本 |
|---|------|--------------|
| 6 | 精选文章挑哪几篇（等 92 篇迁入后再挑） | v1.2 |
| 7 | 头像图素材 | v1.2 |
